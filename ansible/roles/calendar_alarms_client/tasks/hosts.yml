- name: Add static host entry for nas.dixon.net.au to avoid slow AAAA DNS lookups
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: '^\S+\s+nas\.dixon\.net\.au$'
    line: "192.168.20.3    nas.dixon.net.au"
    state: present
  become: true
