- name: Copy bash function files
  ansible.builtin.copy:
    src: "bash_functions/"
    dest: "{{ bash_functions_dir }}/"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0644"
