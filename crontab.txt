# Edit this file to introduce tasks to be run by cron.
#
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
#
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
#
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
#
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
#
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
#
# For more information see the manual pages of crontab(5) and cron(8)
#
# m h  dom mon dow   command

# ====== FETCH CALENDAR DATA ======

# Every 5 minutes between 7am (inclusive) and 9pm (exclusive), but offset from the alarms by 2 minutes
2-59/5 7-20 * * * cd /home/beth/calendar-alarms && ./script/refresh_calendar_data.sh >> /home/beth/calendar-alarms/logs/cron.log  2>&1

# ====== ALARMS ======

# Between 7am (inclusive) and 9pm (exclusive) check for alarms every 5 minutes from 0 to 45 minutes past the hour
*/5 7-20 * * * cd /home/beth/calendar-alarms && ./script/check_for_alarms_nas.sh "5" >> /home/beth/calendar-alarms/logs/cron.log  2>&1

# ====== ANNOUNCEMENTS ======

# 7:23 on weekdays (avoid any alarms)
23 7 * * 1-5 cd /home/beth/calendar-alarms && ./script/morning_announcements_nas.sh >> /home/beth/calendar-alarms/logs/cron.log  2>&1

# 8:33 on weekends
33 8 * * 0,6 cd /home/beth/calendar-alarms && ./script/morning_announcements_nas.sh >> /home/beth/calendar-alarms/logs/cron.log  2>&1

# ====== LOGS ======

# Clean up app logs on Sunday mornings
0 2 * * 0 cd /home/beth/calendar-alarms && ./script/truncate_logs.sh
